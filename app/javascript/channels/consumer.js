// Shared Action Cable consumer. Import this rather than creating your own so
// the app opens a single WebSocket.
import { createConsumer } from "@rails/actioncable"

export default createConsumer()
