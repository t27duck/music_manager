<%# The record was rolled back, so the song is still there; just report why. %>
<%= turbo_stream.append "toasts" do %>
  <%= render "shared/toast", level: :alert, message: "Could not delete #{@song.title}: #{message}" %>
<% end %>
