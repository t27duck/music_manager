<%# One song's artwork. `size` is :large (modal) or :small (list row). %>
<%= render "songs/album_arts/thumbnail",
      song_id: song.id,
      checksum: song.album_art_checksum,
      alt: "Album art for #{song.title}",
      size: size %>
